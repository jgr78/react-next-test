import Title from "./partials/title";

export default () => (
    <section id="section_contact" className="full_width section">
        <div id="contact" className="section_container">
            <Title titleName="Contact" titleStyle="inverse"/>
        </div>
    </section>   
)
